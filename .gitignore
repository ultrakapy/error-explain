# ==============================================================================
# Git Ignore: Idiomatic for Go projects
# ==============================================================================

# -----------------------
# Build artifacts / binaries
# -----------------------
# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary produced by `go test -c`
*.test

# Generic build output directories (common)
 /bin/
 /out/
 /build/
 /dist/
 /_build/

# When using `go install` in module mode, binaries can land in ./bin
bin/

# -----------------------
# Go package objects & caches
# -----------------------
# Compiled package objects (often used in GOPATH-style projects)
pkg/

# Go tool cache (usually outside repo, but ignore local caches if created)
.gocache/

# Coverage and profiling outputs
coverage.out
*.coverprofile

# -----------------------
# Dependency / vendoring
# -----------------------
# Vendor directory is usually committed. Uncomment the next line if you vendor but do NOT commit vendor/
# /vendor/

# Godep / legacy vendor managers
Godeps/
Godeps.json

# -----------------------
# Generated files
# -----------------------
# Common folders for generated sources (adjust to your generator)
gen/
generated/
bindata.go

# -----------------------
# Editor & IDE
# -----------------------
# VS Code - ignore all but allow recommended workspace files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains (Goland / Intellij)
.idea/

# General editor temp files
*.swp
*~
.history/

# -----------------------
# OS files and misc
# -----------------------
.DS_Store
Thumbs.db

# Logs
*.log

# -----------------------
# C/C++ / build-system leftovers from original (safe to keep)
# -----------------------
*.o
*.obj
*.a
*.la
*.lai

# -----------------------
# Important: do NOT ignore these files (track them in git)
# -----------------------
# - go.mod and go.sum should normally be committed for reproducible builds.
# - go.work (if used) is usually committed for multi-module repos.
# (Don't add them to .gitignore)

# -----------------------
# Helpful notes
# -----------------------
# - If you intentionally commit /vendor/, leave it tracked; otherwise uncomment the vendor line above.
# - If your project uses other generated assets or tool-specific folders, add them here (e.g. delv, protoc outputs, etc.).
